# Loop Memory
Updated continuously by all agents as they discover things.
## Learnings
- [frontend-design-overhaul, T4-exec] T4 DEPLOYED (style.css + app.js + admin.js, live on :4533): 18 transition-gap selectors tokenized (--dur-fast/--dur + --ease, property-specific, 30 total transition decls, 0 'all'; research prose "19" was a count slip — its own list = 18); global :focus-visible 2px var(--focus,#8ab0ff) + @supports fallback right after [data-theme=light]; prefers-reduced-motion blanket block as LAST cssRule (animation: none !important; transition: none !important); .fullscreen-player padding → calc(24px + env(safe-area-inset-bottom)); 320px cosmetics SKIPPED (new breakpoints forbidden by research, C5 passes anyway). aria-pressed STRING ternaries at 10 app.js sites + 2 admin.js sites (el() boolean gotcha). Playwright env quirk: browser page-zoom ~0.8x → computed outlineWidth 1.6px for a 2px rule (prove with inline-outline probe; color rgb(138,176,255) exact). admin.js userForm toggle sync() flips classes only → its aria-pressed is initial-state-only (additive constraint; login-screen toggle re-renders so it stays truthful).
- [frontend-design-overhaul, T1] CORRECTED PALETTE — CRITICAL for T2-T4, every ratio re-computed exactly (node, WCAG 2.1) and verified by auditor + verifier: accent FILL #3865f8 = 4.79:1 with white — #4d7dff = 3.69 FAILS as fill, keep it for accent TEXT only (#2f5fff = 5.0 also valid); outline #6d6d75 = 3.08 min on ramp-3 (via #6a6a70 = 2.94 FAILS); green split: fill #15803d = 5.02:1 / text+icons #1db954 = 7.6:1; faint #8a8a93 passes ALL ramps (4.61 on #121218 / 5.01 on #1b1b20 / 5.45 on #121218); elevation ramp #121218→#1b1b20→#22222a; on-surface #f5f5f7 (14.50–17.14 on ramp). --hover #24242b = 1.21:1 but EXEMPT from 3:1 (WCAG 1.4.11 pointer indicator). T3 verifier re-measuring will get these exact decimals — do NOT re-quote the legacy-ramp pairings (#0b0b0f/#121216-keyed values in proposal §3/§10 are a pairing slip; verdicts unaffected).
- [frontend-design-overhaul, T1] Selector distinctions for T3 proof: `.btn-accent` is the accent-fill target (live rgb(97,141,255)/#fff = 3.11 FAIL); `.bottom-bar` is the player-bar proof target (bg var(--surface), live rgb(18,18,22)) — `#player-bar` is a TRANSPARENT wrapper, useless for proof; `.buy-btn` = loja green (live rgb(29,185,84)/#fff = 2.59 FAIL); 12px faint proof targets: `.sidebar-user-handle`/`.playlists-empty` (live rgb(107,107,115) = 3.22–3.72); `.admin-row` border rgb(38,38,47); scrollbar is var(--grid) #2a2a2a — `::-webkit-scrollbar-thumb` must be verified via document.styleSheets cssRules scan (getComputedStyle can't read pseudo-element there).
- [frontend-design-overhaul, T1] PWA overlay (pwa.js): shows 1.5s after EVERY page load/navigation, no dismissal flag persisted — dismiss via `getByRole('button', { name: 'Agora não' })` after each navigate+wait before ANY interaction (pwa-card intercepts pointer events → click timeouts); or assert overlay:false via evaluate before screenshots.
- [frontend-design-overhaul, T1] pm_token gotcha: playwright context PERSISTS localStorage pm_token across navigations (session stays logged-in admin) — logged-out screenshots require `localStorage.removeItem('pm_token')` + reload first. Contrast-math gotcha: 3-digit hex (#fff) breaks slice-based hex parsers — expand to 6 digits (c.split('').map(ch=>ch+ch).join('')) before computing.
- [frontend-design-overhaul, T1] Screenshot convention: before-*.png named by view+viewport — before-home/loja/admin/player at 1440+375, delivered to PROJECT-ROOT loop-reports/ (take_screenshot paths are relative to server CWD = project root, so `loop-reports/before-*.png` works directly). Player shots: before-player-*.png. Loja renders catGrid + .buy-btn publicly WITHOUT any token — no throwaway-client grant needed for loja QA.
- [frontend-design-overhaul, T1-exec] Live view quirks: `.sidebar-nav-label` and `.track-list-header` are ABSENT on logged-in home (not on that view) — don't wait on them during QA.
- [frontend-design-overhaul, T2-exec] T2 DEPLOYED: 2-layer :root (research block verbatim) + [data-theme="light"] + 27 hex→var conversions + 3 clamps (.page-title 30/3vw+1rem/48, .detail-title 36/3.5vw+1rem/48 + its ≥640 MQ reconciled to the same clamp, .login-brand h1 30/2.5vw+1rem/40) + --lh-ui 1.45 on body. ANOMALY: `.btn-icon-lg:hover` (was L796) is a hover-text #fff rule in the hex inventory but missing from BOTH the research 26-row table AND the deferred list — converted to var(--text, #fff) to keep research FAIL-condition A.1 green; document in design-t2.md. All 10 legacy aliases kept; --hover literal #24242b; --text/--danger now defined; loja h1 clamp deferred to T3 (embedded loja.html <style>).
- [frontend-design-overhaul, T2-exec] QA gotchas (consolidated): (1) SW stale-cache — after rebuild+restart the persisted playwright context STILL serves OLD CSS via the PWA service worker; FRESH contexts do NOT always sidestep it in this repo → always `navigator.serviceWorker.getRegistrations().then(r=>r.forEach(x=>x.unregister()))` + caches.delete('*') + reload before live verification; freshness gate = served style.css SHA256 == `git show HEAD:web/assets/style.css` SHA256 (byte-identical) — mismatch means stale serve, not a code bug; (2) truncated JWT pasted into localStorage → fake 401s (app clears pm_token on 401) — use full-length token or register in-page via fetch; (3) `curl.exe -d "..."` mangles JSON quotes in PS — use `--data @file` with an ascii body file; (4) throwaway user 11999998888 (client, 2 categories) created for T2 QA — DELETE on next admin session. Register endpoint also serves categories at GET /api/store/categories.
- [frontend-design-overhaul, T2-exec] Live-verified after deploy: getPropertyValue --accent #4d7dff / --grid #6d6d75 / --faint #8a8a93 / --text #f5f5f7 / --surface #121218 / --surface-2 #1b1b20 / --hover #24242b; .bottom-bar rgb(18,18,24); body rgb(245,245,247); .page-title 48px, .detail-title 48px (lh 57.6 = 1.2x), .login-brand h1 40px (all clamp max at 1100+ viewport, ranges respected); scrollbar thumb var(--grid)/hover var(--outline); 0 console errors; served style.css SHA == tree SHA (byte-identical). Detail route for computed checks: #/category/<id> → .detail-title (renderCategory); log out via localStorage.removeItem('pm_token') + reload for login view.
- [frontend-design-overhaul, T2→T3] DEFERRED-TO-T3 LIST (must land in G3, style.css only): component fills flip to tokens — .btn-accent/.login-submit/.card-play/.player-btn-main → --accent-fill #3865f8; .buy-btn/.chip/.badge (loja green) → --accent-2-fill #15803d (green split: fill #15803d / text+icons #1db954); rgba() color washes (volume-slider, progress-fill, modal overlays) → tokenized; admin/modal polish (inline hexes at .admin-row/.modal/.modal-scroll + pre-existing var() fallbacks); loja.html embedded-style hexes + its h1 clamp: target clamp(26px, 2.5vw + 0.9rem, 42px) (current h1 font-size 26px at loja.html:64). Interim acceptance already documented: #4d7dff fills at 3.69:1 FAIL until T3 flips them.
- [frontend-design-overhaul, T2-exec] loja.html is STANDALONE: lives at web/assets/loja.html, embedded <style> block (lines ~38-107), NO <link> to style.css — it inherits only :root CSS variables (--accent/--subtext/--faint in use) and bypasses style.css component styles; T3 unification target = extract its styles into style.css + clamp its h1, then loja.html renders consistent with the app shell.
- [frontend-design-overhaul, T3-exec] T3 DEPLOYED: 9 accent-fill flips (.btn-accent/.login-submit/.card-play/.pwa-btn.primary/.pwa-icon/.upload-progress-fill/.spinner/.volume-slider/.progress-fill+::after → var(--accent-fill, #3865f8)); loja green fills (.buy-btn/.login-box button → var(--accent-2-fill, #15803d)); KEPT player-btn-main/fullscreen-btn-main white (21:1); chip/badge = TEXT tokens (--accent-2/--warn) + 12px floor, NOT fills; 11 wash tokens added to :root (--border-soft/--scrim/--overlay-scrim/--pwa-scrim/--topbar-glass/--wash/--wash-strong/--wash-soft/--accent-wash/--accent-wash-soft/--warn-wash) + light-theme wash overrides; 16 wash sites + 5 shadow sites converted (decorative gradient rgba kept literal — A.1 permits non-hex); .card → surface-2 + outline border + radius-md 10 + translateY(-2px) hover; .modal → surface-5 #121218 + outline border; .admin-row border outline; loja.html UNIFIED: plain ./style.css link (NEVER ?v=__ASSET_VERSION__ — substitution is index.html-only per static.go), body class="loja" scoping (.card/.login-box collisions resolved), embedded <style> deleted, all rules ported as body.loja block, h1 clamp(26px,2.5vw+0.9rem,42px); .vol-icon polish (inline-flex + subtext). Verified live: btn-accent rgb(56,101,248), buy-btn rgb(21,128,61), h1 42px@1440/26px@375, 0 console errors, 6 after-*.png screenshots, go build PASS x2.
- [frontend-design-overhaul, T3-exec] Hash-comparison gotcha: PS text-based SHA of curl.exe output vs file content gives FALSE mismatches (console decoding mangles bytes) — for the served-vs-tree freshness gate use `curl.exe -s -o $env:TEMP\file` + `Get-FileHash` on both FILES (byte-exact). Also: .track-add:hover/.modal-item.new/.modal-check input legacy green fallbacks var(--accent, #1db954) repointed to var(--accent-2, #1db954) (semantic green, same value); loja thumb gradient hexes #24242e/#101018 tokenized via var(--surface-4, …)/var(--surface-1, …) fallbacks to keep gate A.1 hex grep green; loja 7th ID loginMsg confirmed live-relevant.
- [frontend-design-overhaul, T3] CONSOLIDATION — T3 facts live in the two T3-exec entries above (9 accent-fill sites, wash tokens, elevation, loja unification, hash gotchas); not duplicated here. Remaining durable facts: (1) untracked `play-music.exe~` (21 MB go-build backup artifact at repo root, written during T2/T3 rebuild cycles) — not source, safe for human delete; (2) throwaway phone 11999998888 (client, 2 categories, T2-created) STILL NOT DELETED — T3 credential scan re-hit it benignly, delete on next admin session; (3) .player-btn-main (1250/1251)/.fullscreen-btn-main (1435/1436) #fff/#000 = 21:1 deliberate white player identity — KEEP white in any future contrast task (flipping to accent would FAIL, not fix).
- [frontend-design-overhaul, T4] CONSOLIDATION — T4 facts live in the T4-exec entry above (18 transitions, reduced-motion, focus-visible, safe-area, 12 aria-pressed string sites, page-zoom quirk). Added here: (1) matchMedia-vs-innerWidth drift — at innerWidth 767, `matchMedia('(max-width:767px)')` = false (~1-3px media-viewport drift) → responsive QA MUST assert via matchMedia (or probe 760/800), never innerWidth==boundary; (2) 320px C5 gate ALREADY passed at T3 baseline on all 7 views (scrollWidth==320) — 320px work was cosmetics-only, correctly skipped (no new breakpoints allowed); (3) credentials: zero passwords/tokens anywhere in this loop's MEMORY/STATUS/deliverables (grep clean; only mechanism names, admin@playmusic.com identifier, throwaway phone 11999998888 — delete on next admin session).
