# Loop Plan
## Mode
patch
## Goal
Melhorar o design do frontend por completo: visual system (cores, tipografia, espacamento, tema claro/escuro, consistencia), layouts e componentes (home, loja, admin, biblioteca, player bar, listas de musicas), responsividade (<768px ate desktop), animacoes e micro-interacoes, e acessibilidade (contraste AA, focus states, aria). Evoluir o design existente em style.css/index.html/loja.html/admin — patch, sem reescrever; manter todos os seletores usados pelo JS. Sem auto-commit: mudancas revisadas por humano.
## Stop Condition
all tasks in loop-stack/frontend-design-overhaul/PLAN.md checked
## Budget
20 turns
## Git Integration
no
## Tasks
All 4 tasks run SEQUENTIALLY ([G1] → [G2] → [G3] → [G4]) — style.css is a single monolithic file; each group mutates it, so no parallelism. CODE CHANGES ONLY in `web/` (style.css, index.html, loja.html; T4 may add aria attrs in app.js templates — additive, no logic changes). JS-coupled selector allowlist (RESEARCH.md "Context & Prior Work": 18 IDs + 12 classes + 2 structural + 14 state + 3 data-attrs) is a hard contract — NO renames.

SHARED RULES (apply to every code task T2–T4):
- Worktree: edit in `git worktree add --detach ../play-music-design-wt HEAD` (repo root). NEVER edit `.env`, `auth/`, `payments/`, `secrets/`, `credentials/`.
- Deploy to main tree: copy ONLY the changed `web/` files from the worktree to the main tree (never touch `web/assets/player.js`, `internal/server/helpers.go`, `internal/store/common.go`, `internal/store/users.go` — 4 uncommitted fixes from previous loop), then REBUILD `go build -o play-music.exe .` (workdir=project root) + RESTART :4533: find PID on :4533 (`Get-NetTCPConnection -LocalPort 4533` → OwningProcess), `Stop-Process` it, reload env from `.env` into the process environment (per global MEMORY.md restart recipe), start `.\play-music.exe`, wait `curl.exe -s -o NUL -w "%{http_code}" --max-time 5 http://localhost:4533/` → 200.
- Diff artifacts: `cmd /c "git diff -- <changed files> > loop-reports/design-tX.diff"` (LF-safe; PowerShell `>` writes UTF-16LE — never use it).
- After each deploy: dispatch verifier sub-agent for live QA against :4533 (fresh playwright context, dismiss PWA overlay: wait 1.5s → click "Agora não"), assert 0 console errors, computed-style spot checks via `getComputedStyle`.
- Max 3 fix attempts per item, then escalate to human (log to loop-ledger.json). No auto-commit.

- [x] [G1] T1 — Design audit + proposal (NO code changes). (a) Live evidence: playwright against http://localhost:4533/ — fresh context, dismiss PWA overlay (wait 1.5s → click "Agora não"), capture screenshots with EXPLICIT filenames under `loop-stack/frontend-design-overhaul/loop-reports/before-*.png`: home, loja (navigate to /loja.html), admin (login flow: toggle to "Administrador", creds from prior loop context — never write creds to files), bottom player bar + fullscreen player — at 1440px AND 375px (resize 375x667). Also capture programmatic computed-style evidence (getComputedStyle) for the 4 known contrast failures: #fff on --accent #618dff (3.11:1), #fff on loja #1db954 (2.59:1), --faint #6b6b73 labels (3.28–3.72:1), borders #2a2a2a/scrollbar <3:1 — record measured values in the proposal. (b) Write `loop-reports/design-proposal.md` with ALL sections: current-state findings (incl. contrast audit numbers above), token architecture (primitives + semantic two-layer in :root; data-theme="light" block; legacy --bg/--surface/--accent etc. kept as aliases; var(--x, fallback) safety net; define --text/--danger), target palette (dark elevation ramp #121218 → #1b1b20 → #22222a, on-surface #f5f5f7, sub #b0b0b8, faint ≥ #8a8a93, accent fill darkened ~#4d7dff (≥4.5:1 white text), loja green ~#15803d (≥4.5:1 white text), --outline ≥3:1 vs bg, --focus #8ab0ff; theme-color meta + manifest stay #0B0B0F), typography scale (fs-xs..3xl tokens, clamp() on the 4 display sizes: page-title, detail-title, loja h1, login-brand h1; lh 1.35–1.5 UI / 1.2 titles; 12px label floor), spacing (space-1..8 + page-bottom) / radius (sm/md/lg/pill/round) / shadow (1..3) scales, component-by-component redesign list (~29 components from RESEARCH.md inventory: sidebar, mobile topbar/overlay, cards, genre-cards, empty-state, tabs, track rows, detail header, track-list-header, section-block, back-link, playlist rows, login screen, search bar, bottom player bar, fullscreen player, settings, upload form, queue, spinner, loading-screen, login-toggle, track-add, modal system, admin toolbar/table/chips/badge, PWA popup — with the specific style intent per component), responsive plan (320px guard, safe-area-inset-bottom on bottom bars, keep 640/767/768 MQs), animation plan (transitions ≤0.25s, transform/opacity only, no transition:all; prefers-reduced-motion kill block at file END), a11y plan (global :focus-visible 2px outline var(--focus) offset 2px + @supports fallback, aria-pressed on play/like, aria-label on icon-only buttons, tabular-nums on durations). Deliverable: `loop-reports/design-proposal.md` + `loop-reports/before-*.png` screenshots. No worktree/rebuild needed (no code changes).

- [x] [G2] T2 — Visual system implementation (CODE CHANGE in `web/assets/style.css` only). In worktree: (a) extend `:root` with two token layers — primitives (e.g. --blue-500, --gray-* ramp, --green-*) + semantic (--surface-1..5, --on-surface, --on-surface-sub, --on-surface-faint, --accent, --on-accent, --danger, --warn, --outline, --focus, --radius-*, --space-*, --shadow-*, --fs-*, --lh-*, --dur-fast 0.15s / --dur 0.2s / --ease) — while KEEPING every existing token name as an alias (`--bg`, `--surface`, `--surface2`, `--grid`, `--hover`, `--accent`, `--accent-hover`, `--subtext`, `--faint`, `--font-sans`) so JS-set inline styles and all existing var() refs keep working; (b) add `[data-theme="light"] { ... }` semantic block (additive only — default stays dark per ND_DEFAULTTHEME=Dark); (c) define `--text` and `--danger` in :root (currently only fallbacks); (d) replace hardcoded hex in the main surface/text/border color rules with `var(--x, fallback)` tokens — target: NO raw hex in top-level color rules (keep fallbacks); (e) typography size/weight tokens + `clamp()` on the 4 display sizes. Do NOT redesign components (that's T3). Do NOT touch JS-coupled selectors. Deploy per SHARED RULES (copy style.css to main tree, rebuild, restart). Verification: grep for hex-outside-tokens in style.css; computed-style spot check after deploy (getPropertyValue for --accent/--surface-2/new tokens resolves). Deliverables: `loop-reports/design-t2.diff` (cmd /c LF diff of web/assets/style.css) + `loop-reports/design-t2.md` (what changed, why, contrast numbers).

- [x] [G3] T3 — Layouts & components redesign (CODE CHANGE in `web/assets/style.css` + `web/loja.html`). In worktree: apply design-proposal.md component-by-component — sidebar, mobile topbar/overlay, cards/genre-cards, tabs, track rows, detail headers, login screen, bottom player bar, fullscreen player, settings, upload forms, modals, admin rows/chips/badge, PWA popup, queue, spinner — using the new tokens + elevation ramp (surface-1..5 mapping per proposal). FIX the 4 contrast failures: accent button fill darken (e.g. ~#4d7dff) so white button text ≥4.5:1; loja green buttons (~#15803d or dark text) ≥4.5:1; --faint labels lifted to ≥4.5:1; borders/scrollbar ≥3:1 where the border is a control indicator (--outline), decorative dividers may stay subtle. UNIFY loja.html: remove its embedded `<style>` block, route all styling through style.css tokens using classes already present or minimal NEW classes (additive, keep loja.html JS behavior + its 6 JS-coupled IDs: loginBox/loginMsg/phoneInput/loginBtn/catGrid/packsGrid/packsSection untouched; keep `var(--accent, #1db954)` legacy fallback removal in mind — unify accent to blue or keep green as a semantic --accent-2). Keep every JS-coupled selector working (grep each allowlist member in patched CSS). Deploy per SHARED RULES (copy style.css + loja.html to main tree, rebuild, restart). Verification: verifier live QA on home/loja/admin views (0 console errors), computed contrast re-measurement of the 4 pairs (before/after numbers in the .md). Deliverables: `loop-reports/design-t3.diff` + `loop-reports/design-t3.md` (what changed, why, contrast before/after).

- [x] [G4] T4 — Responsive + animations + accessibility polish (CODE CHANGE in `web/assets/style.css` + `web/index.html` if needed; additive aria attrs in app.js templates only — NO JS logic changes). In worktree: (a) 320px min-width guard (no horizontal scroll below 320px — C5 gate: `document.documentElement.scrollWidth <= innerWidth` at 320px); (b) `padding-bottom: env(safe-area-inset-bottom)` on bottom player bar + fullscreen player (PWA iOS notch); (c) breakpoint review of 640/767/768 MQs (keep, adjust only if verified broken); (d) smooth transitions on hover/focus for interactive elements (≤0.25s, transform/opacity/background only — never `transition: all`); (e) `@media (prefers-reduced-motion: reduce)` block at file END (after base rules) killing spinner/loading/card-play/transitions; (f) global `:focus-visible { outline: 2px solid var(--focus); outline-offset: 2px; }` + `@supports not selector(:focus-visible)` fallback, never `outline: none` on interactive; (g) aria-pressed on play/pause/like toggles + aria-label on icon-only controls (index.html/loja.html static markup; app.js el() templates get aria attrs only — do NOT change JS logic, do NOT touch player.js); (h) `font-variant-numeric: tabular-nums` on duration/seek texts; (i) contrast re-audit pass (all 4 failing pairs now ≥4.5:1 text / ≥3:1 UI). Deploy per SHARED RULES (copy changed files to main tree, rebuild, restart). Verification: verifier live QA — 320px scroll check, focus-visible visible on keyboard tab, reduced-motion emulation, aria-pressed present in a11y snapshot, 0 console errors. Deliverables: `loop-reports/design-t4.diff` + `loop-reports/design-t4.md` (what changed, why, final contrast table).
