# Global Loop Memory
Shared across all loops in this project.
## Learnings
- [validate-play-music-v116, T2] Windows shell: PS `>` writes UTF-16LE — never redirect git output to files; use `cmd /c "git diff ... > file"` for LF diffs. `git worktree add` must use `--detach` (non-detached fails when the branch is checked out elsewhere); remove with `git worktree remove --force`. `git apply` handles LF→CRLF fine (autocrlf=true).
- [validate-play-music-v116, T2] player.js play/pause desync root cause: AbortError rejection fires during src-swap while `switching=true`; the WIP's unconditional `switching=false` disarmed the pause-event guard. Fix = restore `if (!switching)` guard in playAudio() catch (player.js:117-123); never clear `switching` unconditionally in the catch.
- [validate-play-music-v116, T2] QA hook: `window.__player = { getState: () => state, audio }` at web/assets/player.js:324 — assert player state programmatically via `playwright_browser_evaluate`.
- [validate-play-music-v116, T1] Dependabot stale-PR root cause: commit b2e353f8 (2026-08-05) slimmed go.mod (removed goose/jwx/go-sqlite3/hashstructure), rewrote Dockerfile and deleted .github/ — all PRs touching those files conflict with master (all 6 CLOSE). Test with `git merge-tree --write-tree origin/master <branch>` (exit 1 = conflict; conflict lines go to stderr → `2>&1`). `go mod tidy -diff` exits 1 when a delta exists — expected, not an error.
- [validate-play-music-v116, T1] Server runs on http://localhost:4533/ and is healthy (HTTP 200, 0 console errors); gh CLI is ABSENT — map PRs→branches via `git ls-remote origin` `refs/pull/<n>/head`.
- [validate-play-music-v116, T3] Live server serves HEAD, NOT new code: assets are go:embed-embedded into the binary at build time — browser QA of a JS fix is impossible until the human applies the fix diff + rebuilds + restarts; verify with SHA256 of served JS vs `git show HEAD:web/assets/player.js`.
- [validate-play-music-v116, T3] Throwaway-client QA recipe for this repo: public `POST /api/store/register {"phone":"<throwaway>"}` → token → `localStorage.setItem('pm_token', token)` → reload; idempotent per phone, re-call with `categoryIds:[<id>]` to grant categories to the same user; remember to delete the user afterwards.
- [validate-play-music-v116, T3] vision subagent is NOT callable inside loop agent contexts (executor sessions lack the task tool and image input) — use programmatic checks (window.__player.getState(), scrollWidth<=innerWidth, element-overlap scans) and leave screenshots for human/orchestrator vision follow-up.
- [validate-play-music-v116, T4] Admin user CRUD bug (BUG-1, record-only): duplicate email on create/edit returns 500 "Erro interno" — handleStoreError (helpers.go:78-89) has no unique-violation→4xx mapping; any future user-CRUD fix must map unique violations to 400/409.
- [validate-play-music-v116, T4] Admin user guard order in handlers_api.go: self-edit 400 (597-601) fires BEFORE the last-admin count-guard 400 (616-626); self-delete 400 at 675-686. The count-guard is NOT live-triggerable (self-edit blocks it first with 1 admin) — verify statically.
- [validate-play-music-v116, T4] v1.16.0 admin features FULLY QA'd live with real creds (create, tipo-edit with field cleanup, guards G1-G4) and T3's C4 admin-item-render gap is RESOLVED — checkbox legitimately ticked; seed admin's live email is admin@playmusic.com (auth.go bootstrap is only the default).
- [frontend-design-overhaul, T1] play-music frontend CORRECTED dark-theme palette (exact WCAG 2.1 ratios, auditor+verifier confirmed): accent fill #3865f8 (4.79:1) — NEVER #4d7dff as white-text fill (3.69 FAIL, text-only); outline #6d6d75 (3.08 min on ramp-3); green split fill #15803d (5.02) / text #1db954 (7.6); faint #8a8a93 (passes all ramps); elevation ramp #121218→#1b1b20→#22222a; on-surface #f5f5f7. Any future frontend contrast fix in this repo should use these values.
- [frontend-design-overhaul, T1] Playwright MCP context PERSISTS localStorage pm_token across navigations in this repo (session stays logged-in admin) — logged-out screenshots need `localStorage.removeItem('pm_token')` + reload first.
- [frontend-design-overhaul, T1] Loop deliverables convention: screenshots/proposals go to PROJECT-ROOT `loop-reports/` (take_screenshot paths are relative to server CWD = project root; `loop-reports/before-*.png` works directly) — NOT inside `loop-stack/<loop>/`; name views by before-<view>-<viewport>.png.
- [frontend-design-overhaul, T2] QA after any rebuild/restart in this repo: the persisted playwright context may keep serving OLD CSS via the PWA service worker — fresh browser contexts do NOT always sidestep it. Always unregister SWs + delete caches + reload before live design verification; freshness gate = SHA256 of served /style.css == SHA256 of `git show HEAD:web/assets/style.css` (byte-identical), mismatch means stale serve, not a code bug.
- [frontend-design-overhaul, T2] loja.html (web/assets/loja.html) is a STANDALONE page: embedded <style> block, does NOT link style.css — inherits only :root CSS vars (--accent/--subtext/--faint); its h1 is font-size 26px (line 64). Any design-unification task must treat it as a separate target (extract styles into style.css, clamp h1 to clamp(26px, 2.5vw + 0.9rem, 42px)).
- [frontend-design-overhaul, T2] T3 deferred-contrast inventory (style.css): component fills must flip to --accent-fill #3865f8 (.btn-accent/.login-submit/.card-play/.player-btn-main) and --accent-2-fill #15803d (.buy-btn/.chip/.badge — green split fill #15803d / text+icons #1db954); remaining work = rgba() washes, admin/modal inline hexes + var() fallbacks, loja.html embedded-style hexes; interim state #4d7dff fills are 3.69:1 FAIL until flipped.
- [frontend-design-overhaul, T3] loja.html serving quirk: static.go cache-bust substitution is index.html-ONLY — `?v=__ASSET_VERSION__` is never substituted on loja.html; it is served RAW. Any standalone page linking app CSS must use a plain `./style.css` href (placeholder text would ship literally to clients).
- [frontend-design-overhaul, T3] Standalone-page unification pattern: port a page's embedded <style> into style.css scoped under a body class (loja → `body.loja .card` etc.) to kill class collisions (.card/.login-box) without touching shared selectors — and add the class to the page's <body>; keep the page's JS-coupled IDs intact.
- [frontend-design-overhaul, T3] Contrast distinction: `.btn-accent`-family action fills → --accent-fill #3865f8 (4.79:1, MUST flip) but `.player-btn-main`/`.fullscreen-btn-main` stay white #fff/#000 (21:1) — player transport controls are a deliberate white identity, NOT a contrast bug; never flip them.
- [frontend-design-overhaul, final] play-music design system COMPLETE + deployed (all 4 tasks VERIFIED_PASS): style.css carries the 2-layer token architecture (:root primitives+semantic, [data-theme="light"] block), corrected WCAG palette (#3865f8 accent-fill 4.79 / #15803d green-fill 5.02 / #8a8a93 faint / #6d6d75 outline / #f5f5f7 on-surface), 11 wash tokens, clamp typography, motion tokens --dur-fast/--dur/--ease, :focus-visible var(--focus,#8ab0ff) + prefers-reduced-motion block, and loja.html is unified under body.loja. ANY future CSS work on this repo MUST consume tokens (var(--x, fallback)) — raw hexes outside :root/fallbacks fail the A.1 hex-grep gate; T2's old deferred-contrast list and T3's remaining-work inventory are now DONE.
- [frontend-design-overhaul, T4] Responsive QA boundary-drift gotcha: in this repo `matchMedia('(max-width:767px)')` returns false at innerWidth 767 (~1-3px media-viewport drift) — breakpoint assertions MUST go through matchMedia or off-boundary probes (760/800), never innerWidth==boundary.
- [frontend-design-overhaul, T4] el() attribute gotcha: in app.js/admin.js el() templates, boolean attribute values are SKIPPED (false) or rendered as empty string (true) — aria-pressed (any aria-*/boolean attr) MUST be emitted as a STRING ternary `x ? 'true' : 'false'`; booleans produce broken aria.
