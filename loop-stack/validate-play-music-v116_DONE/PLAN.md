# Loop Plan
## Mode
patch
## Goal
Validar os criterios de aceite R1-R3 do ORIGINAL_REQUEST.md, corrigir o WIP de web/assets/player.js (validar comportamento do player e corrigir dessincronizacao do play/pause), revisar os 6 PRs Dependabot abertos (validar e preparar merges seguros) e verificar as features da v1.16.0 sem regressoes. Report-only: sem auto-commit; mudancas sao revisadas e aplicadas por humano.
## Stop Condition
all tasks in loop-stack/validate-play-music-v116/PLAN.md checked
## Budget
20 turns
## Git Integration
no
## Tasks
- [x] [G1] T1 Dependabot PR review: for each open PR #1 #2 #5 #6 #9 #19, verify staleness against origin/master @ 8df05211 and write per-PR verdict (close vs rebase) to loop-reports/dependabot-review.md. Evidence: `git merge-tree --write-tree origin/master <dependabot-branch>` (exit 1 = conflict), target dep/file absent from master go.mod/Dockerfile/.github (goose #2, jwx #5, go-sqlite3 #6, hashstructure #19, Dockerfile osxcross stage #1, `.github/` #9). Re-verify briefly: `go mod verify` (expect "all modules verified"), `go mod tidy -diff` (expect ≤1 cosmetic delta: x/crypto indirect→direct), `go build ./...` (exit 0), `go test ./...` (PASS). No source code changes, no commits. Deliverable: loop-reports/dependabot-review.md (per-PR verdict + go.mod evidence table + close-recommendation summary).
- [x] [G1] T2 player.js WIP fix (CODE CHANGE — git worktree only, main tree untouched): (a) create worktree: `git worktree add ../play-music-loop-wt master` (workdir = project root); (b) capture WIP: `git diff web/assets/player.js > loop-reports/wip.patch` in main tree, then `git apply loop-reports/wip.patch` in worktree; (c) fix the playAudio() catch desync (player.js:114-123): restore the `if (!switching)` guard so a src-swap AbortError rejection can't force `playing=false` mid-swap; (d) validate remaining 6 WIP changes are safe (default volume 0.8, mediaSession handlers calling playAudio()/audio.pause() directly, readyState>0 guards in prev()/seek(), end-of-queue index clamp to `Math.max(0, queue.length-1)`, optimistic pending-seek progress) — fix only if a live risk; (e) verify `node --check web/assets/player.js` passes in worktree; (f) output final unified diff `git diff web/assets/player.js > loop-reports/player-wip-fix.diff` for human review (includes the applied WIP + the fix). Max 3 fix attempts per item, then escalate. No commit, no push. Deliverable: loop-reports/player-wip-fix.diff + loop-reports/wip.patch + short loop-reports/player-wip-fix.md (changes made + rationale + node --check result).
- [x] [G2] T3 R1-R3 browser QA (depends on T2 — QA must test the fixed player): full Playwright session against http://localhost:4533/ using window.__player hook (player.js:324: `getState()`, `audio`): player controls play/pause/seek/volume/progress with state assertions (state.playing/state.progress match audio element after each action), seek before+after metadata, prev restart, next past last track (repeat on/off), mediaSession handlers, volume default 0.8; zero uncaught exceptions + zero console errors (capture `playwright_browser_console_messages` level=error); loja page renders (loja.html catalog); admin page renders (admin.js import); responsive <768px via `playwright_browser_resize` (no overlap/broken elements, check with snapshot + optional vision pass on screenshot); verify valid HTML refs for CSS/JS/images per acceptance criteria. TICK the applicable checkboxes in ORIGINAL_REQUEST.md ONLY for criteria proven in this session (valid syntax = node --check all 6 files; valid HTML refs; player controls; loja+admin render; responsive <768px; hover transitions/contrast). New bugs: RECORD in loop-reports/qa-r1r3.md, do NOT auto-fix. Deliverable: loop-reports/qa-r1r3.md with step-by-step evidence (actions, state snapshots, console output, network statuses, screenshot paths).
- [x] [G3] T4 v1.16.0 feature regression QA (depends on T3): browser QA of new features at http://localhost:4533/: (1) admin user creation/edit — admin: username+email+password no phone; cliente: phone only (verify form fields per type); (2) login by username OR email; (3) tipo editable with field cleanup; (4) last-admin deletion guard — verify guard exists client-side and/or server-side (handlers_api.go:675-686 self-delete, 616-626 demote) but DO NOT actually delete/demote the last admin; (5) thumbnails visible in all lists (mobile + desktop); (6) covers in category forms. Use read-only API checks (GET) + browser; only perform CRUD (create/update/delete category or client user) if safe and reversible; NEVER touch the sole admin account. Record bugs in loop-reports/qa-v116.md, no auto-fix. Deliverable: loop-reports/qa-v116.md with per-feature evidence (screenshots, state, console, API statuses) + bug list with reproduction steps.
