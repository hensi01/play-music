# Loop State — My Project

Last run: 2026-08-11 — **Envs 100% fora do código (branch `feat/envs-only`, worktree play-music-envs, commit 34880b7e, aguardando aprovação de merge)**: .env fora do versionamento + .env.example; loja/baseURL injetados pelo backend via env

## High Priority (loop is acting or waiting on human)

0. **Envs fora do código — branch `feat/envs-only` (worktree `play-music-envs`, commit 34880b7e, AGUARDANDO aprovação de merge)** → pedido do usuário ("quero que as envs não fique no código, somente em envs"). Backend Go já lia 100% via os.Getenv; o problema era: (a) 🔴 **`.env` ESTAVA VERSIONADO no git** com credenciais reais de produção (Bunny S3 secret, Redis, CDN token key, Postgres) → `git rm --cached .env` (arquivo local preservado) + `.gitignore` ganhou `.env`/`.env.*`/`!.env.example` + **`.env.example`** criado com placeholders; ⚠️ **recomendação de segurança (usuário decide)**: as chaves continuam no HISTÓRICO do git (deleção não apaga história) → rotacionar ND_S3_SECRETKEY / ND_REDIS_URL / ND_CDN_TOKENAUTHKEY / DATABASE_URL se o repo já foi exposto; (b) `web/assets/loja.html` tinha STORE_CONFIG (preços/links checkout) hardcoded → movido para env `ND_STORE_CONFIG_JSON`, injetado pelo backend no boot (static.go substitui `__STORE_CONFIG__` em loja.html; fallback `{}` se vazio/JSON inválido via json.Valid); ⚠️ **descoberta**: acentos no JSON do .env quebram com PowerShell 5.1 (lê .env como ANSI → "Cristão" vira "CristÃ£o" e não casa com a API) → solução aplicada: JSON 100% ASCII com escapes `\uXXXX` (ã=\u00e3, ç=\u00e7) documentado no .env/.env.example; (c) `index.html` baseURL fixo → env `ND_FRONTEND_BASEURL` (vazio = mesmo domínio; injetado via json.Marshal — escapa aspas/`</script>`); (d) e2e sem hardcode: `helpers.js` removeu fallbacks `'admin'`/`'123456'` → `requireEnv()` (process.env → .env → erro claro); `upload.spec.js`/`loja.spec.js` usam `BASE` de helpers (PM_E2E_BASEURL) em vez de `http://localhost:4533`. **Evidência**: `go build`/`go vet` OK; `go test ./...` **verde** (server: 9 testes de static incl. 5 novos — injeção válida/vazia/inválida + escape baseURL com `"`/`</script>`); smoke real (servidor de validação :4534 com .env): `/loja.html` → Cristão **R$ 9,90** + link checkout.exemplo.com do env, packs R$ 19,90 ✓, `/` → `baseURL: ""` ✓, placeholder nunca vaza; e2e (desktop, :4534): **10 passed / 1 failed** — a única falha (`loja.spec.js:17` espera 1 card, banco tem 5 categorias) é **PRÉ-EXISTENTE de dados** (confirmado: falha idêntica contra master :4533 sem as mudanças). Verificador (reviewer) PASS 9/9 — nenhum segredo novo no diff. Merge/push **aguardando aprovação** (AGENTS.md); `.env` local já atualizado com as 2 novas envs (ND_STORE_CONFIG_JSON + ND_FRONTEND_BASEURL) — no deploy, Coolify/docker-compose precisam das novas envs também.

0. **Migração MinIO → Bunny Storage Zone `files3` — CONCLUÍDA e no ar** → pedido do usuário ("quero utilizar o cdn storage no lugar do minio"). Storage Zone definitiva: `files3` (endpoint `jh-s3.storage.bunnycdn.com`, accessKey `files3`, bucket `files3` — credenciais do usuário; a zone `play-music` da 1ª mensagem foi descartada). **Zero mudança de código para a migração** (minio-go é S3-compatível) — só `.env` (backup: `var/.env.backup-minio-20260811`; bloco `MINIO_*` marcado OBSOLETO). Migração de dados: script Go temporário (`var/tmp/bunny-migrate/`, removido ao final) — piloto 7 objetos (aprovação do usuário) + cópia total **839/839 objetos, 5.153.259.961 bytes, 0 erros** (verify: ok=839 faltando=0), Content-Type preservado, idempotente. Pull Zone `music.centralcursoss.com.br` conectada ao Storage Zone pelo usuário (F3) — validado: URL assinada (HS256 chave do .env) → **206** (Range), sem token → **403**, TLS OK (problema pré-existente resolvido). Deploy: sem rebuild (binário CDN-only lê o .env) → restart via m1-start.ps1 (PID 27968). Smoke produção: login admin ✓, 832 songs ✓, stream música → 302 → CDN **206 audio/wav** ✓, karaokês 2da2aa58/6e0eb344 → 302 → CDN **206 video/mp4** ✓. ⚠️ **Descoberta**: a storage zone `files3` é COMPARTILHADA com outro projeto (8.615 objetos `videos/` HLS .ts + 845 `playlist.m3u8` + `images/`/`img/`/`offers/`); o scanner importava os `.m3u8` HLS como playlists do app (596 playlists lixo criadas em 8min, master/playlist esvaziadas). **Correção aplicada**: fix `feat/scanner-m3u8` (worktree play-music-m3u8fix, commit 907322f) — scanner ignora `.m3u8` (só `.m3u` é playlist do app); 596 playlists lixo DELETADAS do banco (todas vazias, updated 20:42-20:48; 0 playlist_tracks); rebuild + restart; scan limpo: `skipped=832, duration=5.29s`, ZERO "playlist imported", 0 erros. **PENDENTE**: ~~aprovação do merge/push de `feat/scanner-m3u8`~~ ✅ **FEITO** (merge --no-ff 07828ff6 + push `6c342f96..cbe8d0bc`; docs commitadas em cbe8d0b; worktree removido, branch deletada). **Usuário recriará** na UI as playlists "master"/"playlist" (esvaziadas). ~~Karaokê 2125fe43 "002-faixa"~~ ✅ **REMovido do catálogo** (órfão pré-existente, objeto não existe em nenhum storage; DELETE karaokes + CASCADE category_karaokes; 0 artworks; restam 2 karaokês válidos: 2da2aa58/6e0eb344).

## High Priority (loop is acting or waiting on human)

0. **Fallback para o CDN Bunny com retry no cliente — MERGEADO no master (c3c5ae90) e DEPLOYADO no :4533** → pedido do usuário ("utilize um fallback para o cdn bunny"); push aprovado ("git push" + "continue"). Branch `feat/cdn-fallback` (worktree play-music-cdn-fallback, 3 commits: f0f2e60 feat + 62a3707 fix + 72cc8ca docs reviewer). **`?nocdn=1`** em `/api/stream/{id}` e `/api/karaoke/stream/{id}` força o proxy local (ServeNative/ServeVideo com Range, sem transcode); karaokê **passou a usar o CDN** (302 para URL assinada HS256+expires quando o probe Range passa — antes servia 100% local); `probeCDNRange` com log `cdn probe failed` (warn) e **timeout 3s** (não travar o hot path); frontend: `player.js` ladder de 3 estágios no error (0=normal/CDN → 1=`streamUrlLocal` ?nocdn=1 → 2=format=mp3 transcode; formato não-nativo para imediatamente), `karaoke.js` ladder de 2 estágios (normal → ?nocdn=1; sem transcode de vídeo), `api.js` ganhou `streamUrlLocal`/`nativeFormats` exportado/`karaokeStreamUrl(k, nocdn)`; teste e2e novo "CDN fora do ar → fallback local (?nocdn=1) mantém playback" (passa desktop+mobile); `PM_E2E_BASEURL` no playwright.config.js e helpers.js; README. Validação: go test verde, node --check ok, suíte e2e :4539 = 56/1/3 (3 falhas pré-existentes de dados), karaokê 302+fake CDN reproduz no browser, CDN morto → 200 local + warn, retry ?nocdn=1 no browser; reviewer PASS (parecer em teamwork-state/cdn-fallback-20260811/). Deploy: `play-music.exe` substituído (backup: `play-music-pre-cdn-fallback.exe`), servidor :4533 reiniciado (pid m1-srv.pid), smoke test OK — produção serve local (probe do CDN real falha por TLS, ver Watch List 0) e `?nocdn=1` responde 200.

1. **Auto-orientação do karaokê: gira a tela conforme o aspect do vídeo — 2 commits pushados (aguardando aprovação)** → pedido do usuário ("quando o vídeo for horizontal, gire automaticamente no Android"). Decisões: girar nos 2 casos (w>h → landscape; h>w → portrait) + ajustar também a branch antiga. (a) `fix/player-rotate` (worktree play-music-rotate, commit 71fe3fc, atualiza o PR #29 — agora PR #29 = fix/player-rotate): `karaoke.js` ganhou `autoOrientFromVideo()` — lê `video.videoWidth/videoHeight`, seta `state.orientation` (ícone/aria-label do botão acompanham) e chama `lockOrientation()` (Android Chrome; iOS/desktop no-op) — disparado em `loadedmetadata` e `resize` do vídeo (streams adaptativos); o botão manual continua prevalecendo até a próxima carga. (b) `fix/karaoke-landscape-spacing` (worktree play-music-karaoke-ls, commit c6da49b): a trava landscape INCONDICIONAL ao abrir foi substituída pelo mesmo `autoOrientFromVideo()` condicional (`lockLandscape()` → `lockOrientation(orientation)` genérico; `open()`/fullscreenchange one-shot → `autoOrientFromVideo()`; `loadedmetadata`+`resize`; `unlock()` mantido em close/sair do fullscreen). Validação Playwright com stub de `screen.orientation.lock` (servidores :4537 e :4538): vídeo "003 - Harpa Cristã" (testsrc, horizontal) → `lock('landscape')` ✓ em AMBAS as branches (mobile 375×812); vídeo "dahma - 2.650.000" (vertical, 480×854 real) → `lock('portrait')` ✓ em AMBAS; clique manual continua funcionando após auto-giro ✓ (fix/player-rotate); desktop 1280×720 sem regressão ✓; 0 console errors; `go test ./...` verde; `node --check` ok. Artefatos de validação removidos. **PENDENTE**: aprovação para merge (PR #29 fix/player-rotate + PR do fix/karaoke-landscape-spacing) + rebuild/restart do :4533. OBS: com as duas mudanças, o comportamento final é a trava CONDICIONAL (aspect do vídeo), não mais a trava landscape incondicional.

1. **Botão alternar modo paisagem/vertical (karaokê + fullscreen player) + controles do karaokê com 50px inferior — PR #29 (draft) branch `fix/player-rotate` (worktree play-music-rotate, commits 579c338 + 71fe3fc)** → pedido do usuário. (a) `karaoke.js`: ícones `rotateLandscape`/`rotatePortrait` (ícone = a ação), estado `orientation`, `toggleOrientation()` com `screen.orientation.lock()` (Android Chrome; iOS/desktop no-op silencioso com fallback unlock; `.catch` nunca deixa a UI presa), botão `karaoke-rotate` na `player-side` (junto de velocidade/volume/tela cheia), `sync()` atualiza ícone+aria-label, exposto em `window.__karaoke`; (b) `app.js`: mesmo botão no `fullscreen-top` do player de música (ao lado da Fila), `togglePlayerOrientation()` + `playerOrientation` incluído no `structuralKey()` (re-render do ícone), conteúdo reestruturado em `.fullscreen-info-col` (visual idêntico em portrait/desktop); (c) `style.css`: `.karaoke-controls` padding-bottom `calc(50px + safe-area)` desktop E mobile (era 20px/0px → 50px, pedido do usuário), `.karaoke-rotate { pointer-events:auto }`, media query `@media (orientation: landscape) and (max-width:900px)` → fullscreen-content vira row (capa à esquerda, controles à direita) só em celular deitado (desktop mantém column). Validação Playwright contra servidor :4537 (exe `play-music-rotate.exe`, binário novo): mobile 375×812 → botão nos 2 players ✓, clique alterna aria-label/ícone/estado (id: rotatePortrait↔rotateLandscape) ✓, paddingBottom computado = 50px ✓ (gap real do conteúdo ao fundo = 50px ✓), fullscreen em 812×375 → flexDirection row + art 128px + sem overflow ✓, volta a portrait → column ✓, desktop 1280×720 → media query inativa (column preservado, art 288px) + botão presente e clicável ✓, karaokê desktop 50px ✓, 0 console errors em todos os cenários ✓; suíte e2e completa contra :4537 → 54 passed / 1 skipped / 3 failed — as 3 falhas (loja.spec ×2, responsive.spec) são PRÉ-EXISTENTES de dados: `#catGrid .card` espera 1 e o banco tem 2 categorias ("Cristão" + "Harpa Cristã" adicionada depois que o teste foi escrito); nada relacionado à mudança (loja intocada); go test ./... verde; node --check ok. **PENDENTE**: aprovação para commit/merge + rebuild/restart do :4533 (binário atual ainda é antigo). rot-srv* (gitignored) documentam o boot de validação.
2. **Karaokê: forçar paisagem no mobile + espaçamento inferior dos controles — PR #29 (draft) aguardando aprovação** → branch `fix/karaoke-landscape-spacing` (worktree play-music-karaoke-ls, commit 979f3c0). Fixes: (a) `manifest.webmanifest` tinha `"orientation": "portrait"` → PWA instalado nunca girava para paisagem (removido); (b) `karaoke.js` agora chama `screen.orientation.lock('landscape')` ao abrir (Android Chrome, após fullscreen ativo — caminho direto ou via fullscreenchange one-shot), com `unlock()` em `close()` e ao sair do fullscreen com o player aberto; iOS continua usando o fullscreen nativo do vídeo (já gira sozinho); desktop/headless: falha silenciosa sem quebrar nada; (c) `style.css` mobile: `padding-bottom` dos controles mudou de `env(safe-area-inset-bottom)` (0px na maioria dos Androids → barra colada na borda) para `calc(16px + env(safe-area-inset-bottom))`; (d) `sw.js` CACHE_NAME → pm-shell-v2 (manifest é pré-cacheado; bump garante atualização nos clientes instalados). Validação Playwright contra servidor :4534 (exe `play-music-karaoke-ls.exe`, 2 karaokês no banco): viewport mobile 375×812 → paddingBottom computado = `16px` ✓, `screen.orientation.type` = `landscape-primary` após abrir ✓ (lock aplicado), player fecha sem erros ✓, sair do fullscreen mantém player aberto sem erros ✓, manifest servido sem campo orientation ✓, desktop 1280×720 sem regressão (padding desktop 20px inalterado) ✓, 0 console errors em todos os cenários ✓; go test ./... verde; node --check ok. **PENDENTE**: aprovação para merge + rebuild/restart do :4533 (binário atual ainda é antigo). kls-start.ps1/kls-srv* (gitignored) documentam o boot de validação. OBS: a trava landscape incondicional desta branch foi substituida pela auto-orientacao condicional (commit c6da49b) - ver item 0.

3. **Idioma pt-BR / Español — PR #28 (draft) aguardando aprovação** → branch `feat/i18n` (worktree play-music-i18n, commit 29a2827). Novo `web/assets/i18n.js` (dicionários ~300 chaves, t()/plural/detectLanguage/setLanguage com evento pm:lang-changed, translateBackendError para erros do Go, languageSelector para login/sidebar/config, window.__i18n para pwa/loja). Detecção: navigator.language (pt*→pt, es*→es, outro→pt padrão); manual persiste em localStorage.pm_lang. Traduzidos: app.js (login/sidebar/home/busca/biblioteca/categoria/playlist/curtidas/histórico/fila/config/player), admin.js (tudo), karaoke.js (labels), pwa.js, api.js (erros) e loja.html. Validação Playwright :4536: es-ES→es ✓, en-US→pt ✓, seletor alterna+persiste ✓, telas logadas em es ✓ (sidebar/home/karaoke/admin), loja em es ✓, zero erros de console; go test verde; node --check ok. Bugs corrigidos no caminho: parêntese extra no admin.js (t('admin.noSongMatch')) e dupla instância do i18n (script tag removido do index.html — app.js importa). **PENDENTE**: aprovação para merge + deploy no :4533.

4. **Foto/capa de categoria — verificação e fix da loja** → branch `fix/categoria-foto-loja` (worktree play-music-fix-foto), suíte 57 passed / 1 skipped / 0 failed. Diagnóstico: (a) a loja (loja.html) NÃO usava a foto da categoria — card usava ícone ♫ estático (`<div class="thumb">&#9835;</div>`), nenhum request a /api/artwork; (b) `GET /api/artwork/{id}` exige JWT → 401 sem token, incompatível com loja pública; (c) cache em disco tinha lixo (`ac6333…-96.jpg` = screenshot de página web de upload antigo não invalidado). Teste e2e do upload na categoria "Cristão" (ac6333…): POST photo → 204 + foto aplicada (artwork 300 = 7409B ≠ placeholder 7836B, visão confirmou logo do app), invalidate limpou cache sujo, DELETE → 204 + volta ao placeholder, erros 400 (não-imagem/sem campo), 404 (categoria inexistente), 401 (sem token) — upload 100% funcional. Fix: rota pública `GET /api/store/categories/{id}/photo` (serve capa via artwork.Serve, sem JWT) + loja.html categoryCard com `<img class="thumb-img">` (rota pública, loading=lazy, onerror remove → ♫ de fundo reaparece) + CSS .thumb-img (object-fit: cover, overflow hidden). Testes novos: loja.spec "card de categoria exibe a foto (capa pública, sem JWT)" (img visível + GET 200 image/jpeg sem auth) e admin.spec "upload e remoção de foto de categoria via API" (categoria descartável: artwork placeholder → upload → muda → não-imagem 400 → delete → placeholder) + helpers apiUploadCategoryPhoto/apiDeleteCategoryPhoto/apiArtworkBytes. Validação: go test ./... ok, suíte completa rodada contra servidor de validação :4534 (play-music-fix.exe) — 57/1/0; servidor :4533 ainda com binário ANTIGO (sem a rota pública) — substituir e reiniciar com aprovação humana. Foto aplicada na "Cristão" (decisão do usuário).

5. **Dívidas documentadas (não bloqueantes, decisão de produto)** — B-NEW-2 (register público concede categorias sem prova de pagamento — integrar gateway), categoryIds vazio em /api/admin/songs (category_songs sem backfill; scanner não reindexa; cliente novo vê tudo vazio — B4), overlay PWA re-exibe a cada load (persistir dismiss?), suíte deixa 2 WAVs de teste por rodada no catálogo/bucket (sem API de delete de songs — cleanup manual documentado em e2e/tests/upload.spec.js), transcode format=mp3 sem fixture (seed 100% mp3), ND_FFMPEGPATH não setado no .env. **NOVO**: suíte e2e `#catGrid .card` espera 1 categoria e o banco tem 2 ("Cristão" + "Harpa Cristã") → loja.spec:16 e responsive.spec:44 falham por dados (pré-existente, não relacionado ao fix/player-rotate) — atualizar o teste ou documentar a 2ª categoria.

## Watch List

0. **CDN Bunny = ÚNICA via de entrega (CDN-only) — ATIVO em produção** → pedido do usuário ("quero utilizar somente o cdn bunny, sem fallback"); branch `feat/cdn-only` (worktree play-music-cdn-only, commit 4b9e2e4, merge --no-ff na master). **Removido**: probe Range (`probeCDNRange`/`CDNRangeOK`), proxy local (`ServeNative`/`ServeVideo`/cache `var/cache/stream`), fallback presigned MinIO (`?nocdn=1` ignorado) e a ladder de retry no frontend (player.js/karaoke.js: erro → para de tocar). **Novo**: `StreamURL`/`SignedURL` exigem CDN configurado (500 "CDN não configurado" se não); transcode `?format=mp3` mantido só para formato. Validação: go build/vet/test verde (6 testes stream incl. 2 novos), node --check 3 JS, servidor :4540 (binário novo): nativo → 302 CDN, `?nocdn=1` → ignorado (302), karaokê → 302, wav?format=mp3 → transcode 200, sem CDN → 500 (song+karaokê), transcode sem CDN → 200; e2e player.spec 6/6 (novo teste "CDN fora do ar → para sem retry" com exatamente 1 request); suíte completa 28/1/1 (1 falha pré-existente de dados loja.spec:16 — 2 categorias no banco vs 1 esperada). Deploy: play-music.exe novo (backup: play-music-pre-cdn-only.exe), :4533 reiniciado (PID 13928), smoke OK — song 302→CDN (URL responde 206 Range), karaokê 302, transcode 200, log SEM "cdn probe failed". **PUSH não feito** (aguardando aprovação explícita).
1. **Servidor local** — play-music.exe (binário CDN-only + fix .m3u8, buildado do worktree play-music-m3u8fix) rodando em :4533 (iniciado via teamwork-state/fix-bugs-design-20260808/m1-start.ps1, que carrega .env — o app NÃO auto-carrega .env). Logs em m1-srv.log / m1-srv-err.log (gitignored). **Storage = Bunny Storage Zone `files3`** (S3-compatible, endpoint jh-s3.storage.bunnycdn.com) — MinIO desativado (bloco OBSOLETO no .env, bucket play-music intacto para rollback). Catálogo 832 músicas + 3 karaokês (1 órfão). Entrega 100% CDN: streams → 302 para URL assinada da Bunny (Pull Zone music.centralcursoss.com.br → Storage Zone files3). Servidores de validação (:4540/:4541) já foram parados.
2. **Upload de músicas via e2e** — a suíte (upload.spec) insere WAVs de teste no catálogo; rodadas futuras precisam do cleanup documentado (DELETE LIKE 'pw-e2e%' + objetos órfãos do bucket, senão o scanner reindexa).
3. **Postgres remoto (72.62.11.235)** — kill -Force do servidor deixa transações abertas que seguram locks (DELETEs travam); usar pg_terminate_backend se ocorrer. Migration 0009 (karaokes) aplicada manualmente via script temporário (C:\Users\hensi\AppData\Local\Temp\opencode\migrate-helper) — o boot do binário novo vai registrar sem efeito (já consta em schema_migrations).
4. **Karaoke de teste** — objeto `uploads/<id>.mp4` do vídeo de validação (testsrc, 47KB) ficou órfão no bucket (linha removida do banco; scanner não indexa vídeos, não reaparece). `pw-karaoke-teste.mp4` na raiz do worktree é gitignored.
4. **Front-end regression risk** — redesign + fixes JS commitados; suíte e2e (57 testes) cobre as rotas principais como rede de segurança.

## Recent Noise (ignored this run)

- Deleções pré-existentes de .opencode/agents/knowledge-sources/* e skills/loop-triage/ (anteriores ao run; não commitadas — verificar se são intencionais).
- B5 overlay PWA: comportamento intencional (pwa.js "mostra sempre em toda visita").
- Manifest webmanifest servido como text/plain (deveria ser application/manifest+json) — cosmético.
- .genre-card é dead code no CSS (pré-existente).
- Cache HTTP do navegador (Cache-Control max-age=86400) mascara mudanças de artwork em fetches sem no-store (comportamento esperado, não bug).

---
Run log: 2026-08-13 - SWAP DE HOSTNAME DA CDN CONCLUÍDO (skill cdn-hostname-swap; aprovação "execute" + "Merge + push"): branch feat/cdn-hostname-swap (worktree play-music-cdn-swap, 2 commits 15ae2e2 + efe461d) mergeada --no-ff + push. Contexto: o .env já usava a pull zone NOVA (ND_CDN_BASEURL=https://files.nuvexaia.online, token auth HS256) — o hostname antigo music.centralcursoss.com.br sobrevivia só em comentário/docs. Mudanças: (1) web/assets/index.html:22 — comentário de exemplo do baseURL atualizado; (2) .env.example:39-42 — ND_CDN_BASEURL=https://files.nuvexaia.online (era placeholder); (3) scripts/migrate-urls.sql NOVO — idempotente, BEGIN/COMMIT: Seção 1 MinIO legado (minios3.centralcursoss.com.br) → storage (jh-s3.storage.bunnycdn.com), Seção 2 pull zone antiga → nova, Seção 3 storage INTOCADO; colunas songs.path, karaokes.path, settings.value, playlists.comment, play_queue.data (JSONB via replace(::text)::jsonb); categories.checkout_url excluído de propósito (links de pagamento). Storage files3/jh-s3.storage.bunnycdn.com mantido (endpoint S3, não reescrito). Estado dos agentes (STATE.md/loop-stack/teamwork-state/loop-reports) NÃO reescrito (registro histórico, decisão do usuário). Verificação: go build/vet/test verdes (phone 0.8s/server 2.9s/stream 1.2s); grep — music.centralcursoss resta só em STATE.md + próprio SQL; live: song 302 → files.nuvexaia.online assinado → 206 audio/wav + CORS * , sem token 403, karaokê 302 → 206 video/mp4; e2e login+player 11/11 desktop + 11/11 mobile; reviewer PASS 4/4 (parecer teamwork-state/cdn-hostname-swap-20260813/REVIEW.md). :4533 reiniciado (PID 13300) para a validação. PENDENTE usuário: conferir ND_CDN_BASEURL no painel Coolify + redeploy; rodar scripts/migrate-urls.sql no psql (esperado 0 linhas).
Run log: 2026-08-11 - PENDÊNCIAS DA MIGRAÇÃO FECHADAS (aprovação do usuário "execute"): (1) merge --no-ff de feat/scanner-m3u8 na master (07828ff6) + push origin/master (6c342f96..cbe8d0bc) + commit docs cbe8d0b (README/STATE.md); worktree play-music-m3u8fix removido, branch deletada — master oficializada com o fix .m3u8 (deploy já rodava o binário com o fix). (2) Playlists "master"/"playlist": usuário recria na UI (conteúdo original não recuperável — sem seed no código, sem .m3u no bucket). (3) Karaokê órfão 2125fe43 "002-faixa" REMOVIDO do catálogo via SQL (DELETE karaokes; CASCADE limpou category_karaokes; 0 artworks órfãs; restam 2 karaokês válidos 2da2aa58/6e0eb344 — confirmado via GET /api/karaokes). dbclean (var/tmp) removido.
Run log: 2026-08-11 - MIGRAÇÃO MINIO → BUNNY STORAGE ZONE files3 CONCLUÍDA E DEPLOYADA (pedido do usuário "quero utilizar o cdn storage no lugar do minio"; plano aprovado F0-F7 com ajustes: piloto + logs/progresso): smoke S3 do Bunny (list/put/stat/get-range/remove, região us-east-1 OK); verify inicial revelou files3 com 3.478 objetos pré-existentes de OUTRO projeto (videos/ HLS .ts + .m3u8, images/, img/, offers/) — zero colisão de chaves com a biblioteca; piloto 7 objetos (mp3/wav/mp4/covers, Content-Type corretos, idempotência OK); URL assinada do piloto via Pull Zone → 206 (Range) / sem token 403 / TLS OK; cópia total 839 objetos / 5.153.259.961 bytes / 0 erros / 8 workers / relatório report.txt; verify final ok=839 faltando=0 tamanho-diferente=0; .env switch (backup var/.env.backup-minio-20260811; ND_MUSICFOLDER/ND_S3_* → files3; MINIO_* OBSOLETO); restart :4533 (PID 29888); scan inicial com Bunny expôs BUG: 845 .m3u8 HLS importados como playlists → 596 playlists lixo (master/playlist, todas tracks=0) + master/playlist originais esvaziadas (ImportPlaylist insere duplicado por nome — sem unique constraint); servidor parado para conter o dano; dbcheck (var/tmp) quantificou: 596 playlists, 0 com tracks, 832 songs ok, 0 lixo .ts/.m3u8 como song; correção aprovada: fix scanner.go (só .m3u, .m3u8 ignorado) no worktree play-music-m3u8fix (branch feat/scanner-m3u8, commit 907322f, build/vet/test verdes); DELETE das 596 playlists lixo (0 tracks afetadas); binário novo buildado no worktree → raiz (backup play-music-pre-m3u8fix.exe); restart :4533 (PID 27968); scan limpo: skipped=832 duration=5.29s sem playlist imported; smoke: login admin ✓ songs 832 ✓ stream 302→CDN 206 audio/wav ✓ karaokê 2da2aa58/6e0eb344 302→CDN 206 video/mp4 ✓ (2125fe43 órfão pré-existente, objeto não existe em nenhum storage); STATE.md/README atualizados; var/tmp/bunny-migrate + dbcheck removidos. PENDENTE: merge/push de feat/scanner-m3u8 (deploy já roda o binário com o fix); usuário recria playlists master/playlist na UI.
Run log: 2026-08-11 - CDN-ONLY IMPLEMENTADO E DEPLOYADO (pedido do usuário "quero utilizar somente o cdn bunny, sem fallback"; execução aprovada): branch feat/cdn-only (worktree play-music-cdn-only, commit 4b9e2e4) mergeada na master (--no-ff). Backend: stream.go -220 linhas (remove probeCDNRange/CDNRangeOK/ServeNative/ServeVideo/ensureStreamCache/mediaFile/parseRange/streamChunkSize/cache var/cache/stream/fallback presigned; StreamURL+SignedURL novos com requireCDN() → 500 'CDN não configurado'); handlers_media.go/handlers_karaoke.go: 302 incondicional, ?nocdn=1 ignorado, transcode mantido p/ formato (wav?format=mp3 → 200). Frontend: api.js (streamUrlLocal/nativeFormats export/nocdn removidos), player.js e karaoke.js sem ladder (error → para). Testes: stream_test 6/6 (2 novos: erro sem CDN, usa CDN quando configurado), player.spec 6/6 (novo 'CDN fora do ar → para sem retry' com 1 request), suíte e2e completa 28/1/1 (1 falha pré-existente loja.spec:16 = 2 categorias no banco), go vet/build ok, node --check 3 JS ok. Validação ao vivo :4540/:4541: nativo 302, nocdn ignorado 302, karaokê 302, transcode 200, sem CDN 500 song+karaokê + transcode continua 200. Deploy: backup play-music-pre-cdn-only.exe, play-music.exe rebuildado, :4533 reiniciado (PID 13928, m1-start.ps1 com .env atual), smoke: song 302→CDN (206 Range no destino), karaokê 302, transcode 200, log sem probe warn. PUSH pendente de aprovação.
Run log: 2026-08-11 - TESTE AO VIVO DO CDN BUNNY (pedido do usuário "teste o cdn bunny do projeto"): RESULTADO = CDN FUNCIONAL. go test ./internal/stream/... verde (4/4, assinatura HS256/MD5 bate com vetores); script Go temporário (TEMP/opencode/cdnurl, replica CDNURL()/sign()) validado contra os vetores e usado para gerar URLs frescas: chave do .env (ec090a62) + expires 1h → 206 Range / 200 completo (song uploads/ed472ebe…mp3, 11.7MB) e 206 (karaokê af8b03eb…mp4 via 2da2aa); chave antiga dos vetores (50df94e8) com token fresco → 403 (pull zone usa a chave nova); vetores originais → 403 = expires expirado (1786072596 ≈ 06/08), não chave errada; sem token → 403 (token auth ativo). Servidor de validação :4540 (script TEMP/opencode/start-val4540.ps1, carrega .env ATUAL + ND_PORT=4540, mesmo binário): /api/stream/bcb03701… → 302 para URL assinada HS256, /api/karaoke/stream/2da2aa… → 302; ?nocdn=1 → 200 (audio/mpeg 11.7MB / video/mp4 10.2MB); log :4540 sem "cdn probe failed". Playwright (spec temporário removido após o run, config oficial do e2e): login admin → clicar 1ª música → 1 request a music.centralcursoss.com.br, readyState 4, paused=false, 0 console errors → ciclo browser 302→CDN reproduz. DESCOBERTA CRÍTICA: :4533 (PID 21388) bootou 14:47:50, .env mudou 14:59:15 → :4533 roda com chave antiga, probe dele loga "cdn probe failed status=403" (m1-srv.log 18:31:23) e serve 200 local — precisa RESTART (m1-start.ps1) para o CDN ser usado em produção; validação completa feita na :4540 sem tocar na :4533. Karaokê 2125fe43 ("002-faixa") → 404 CDN / 500 local = objeto órfão no bucket (não é bug do CDN). Artefatos temporários removidos (spec, cdnurl, val4540*, token). Sem mudanças de código nem de .env.
Run log: 2026-08-11 - FALLBACK CDN MERGEADO E DEPLOYADO: push aprovado; merge no master (c3c5ae90, --no-ff) + push; build play-music.exe novo (backup play-music-pre-cdn-fallback.exe), restart do :4533 (m1-start.ps1, pid m1-srv.pid); smoke test: song normal 200 local (probe CDN real falha TLS → fallback), song ?nocdn=1 200 audio/wav, karaoke normal 200 local, karaoke ?nocdn=1 200 video/mp4; warn "cdn probe failed" visível no m1-srv.log. Próxima ação opcional do usuário: corrigir cert/CNAME do pull zone (Watch List 0) para o CDN passar a ser usado.
Run log: 2026-08-11 - fallback CDN Bunny (branch feat/cdn-fallback, worktree play-music-cdn-fallback, commits f0f2e60 + 62a3707): ?nocdn=1 nos streams de áudio e karaokê (proxy local com Range, sem transcode); karaokê agora usa CDN (302 assinado quando o probe Range passa); probe com log warn + timeout 3s; player.js ladder 3 estágios (CDN → local → transcode) e karaoke.js 2 estágios; teste e2e novo + PM_E2E_BASEURL; validação: go test verde, node --check ok, suíte e2e :4539 = 56/1/3 (3 falhas pré-existentes de dados), karaokê 302+fake CDN reproduz no browser, CDN morto → 200 local + warn, retry ?nocdn=1 no browser; reviewer PASS; aguardando aprovação de push/merge/restart do :4533. Descoberta: cert TLS do CDN de produção inválido para o domínio configurado (servindo tudo local).
Run log: 2026-08-10 - auto-orientacao do karaoke pelo aspect do video (2 branches): fix/player-rotate 71fe3fc (autoOrientFromVideo: videoWidth/videoHeight -> lock conforme aspect em loadedmetadata+resize; state.orientation acompanha o botao; manual prevalece ate a proxima carga) e fix/karaoke-landscape-spacing c6da49b (trava landscape incondicional substituida pela condicional; lockLandscape -> lockOrientation generico; open()/fullscreenchange/loadedmetadata/resize; unlock mantido). Validacao Playwright com stub do screen.orientation.lock em :4537 e :4538: video horizontal (003 testsrc) -> lock('landscape') em ambas ✓; video vertical (dahma 480x854 real) -> lock('portrait') em ambas ✓; clique manual apos auto-giro ✓; desktop sem regressao ✓; 0 console errors; go test verde; node --check ok. Aguardando aprovacao de merge/restart do :4533.
Run log: 2026-08-10 — botão alternar paisagem/vertical nos 2 players + 50px nos controles do karaokê (branch fix/player-rotate, worktree play-music-rotate): karaoke.js (ícones rotateLandscape/rotatePortrait, estado orientation, toggleOrientation com screen.orientation.lock + fallback unlock, botão karaoke-rotate na player-side, sync atualiza ícone/aria-label, hook __karaoke.toggleOrientation); app.js (botão rotate no fullscreen-top do player de música, playerOrientation no structuralKey, wrapper .fullscreen-info-col); style.css (padding-bottom karaoke-controls calc(50px+safe-area) desktop e mobile — pedido do usuário —, .karaoke-rotate pointer-events, media query (orientation:landscape) and (max-width:900px) → fullscreen row em celular deitado). Validação :4537 mobile 375×812: botão nos 2 players ✓, alternância ícone/aria/estado ✓, paddingBottom=50px e gap real=50px ✓, 812×375 row com art 128px sem overflow ✓, portrait column ✓; desktop 1280×720 media query inativa + botão funcional ✓; 0 console errors; suíte e2e 54/1/3 (3 falhas pré-existentes de dados na loja: banco tem 2 categorias, teste espera 1); go test verde; node --check ok. Aguardando aprovação para commit/merge/restart do :4533.
Run log: 2026-08-10 — feature Karaoke – Vídeos implementada e validada (branch feat/karaoke-videos): migration 0009 aplicada, backend (store/rotas/stream/artwork/scanner) + frontend (player vídeo fullscreen, /#/karaoke, seção home, aba admin, modal categoria) + 3 testes Go; validação funcional Playwright com upload real MP4 (thumb, player, teclado, 201 com metadados); reviewer PASS; aguardando aprovação de merge e restart do :4533.
Run log: 2026-08-10 — fix karaoke landscape+spacing (branch fix/karaoke-landscape-spacing, worktree play-music-karaoke-ls): manifest "orientation": "portrait" removido (PWA instalado voltou a girar); karaoke.js trava paisagem no Android via screen.orientation.lock('landscape') (após fullscreen ativo; unlock no close e ao sair do fullscreen com player aberto; iOS/desktop: no-op silencioso); controles mobile com padding-bottom calc(16px + env(safe-area-inset-bottom)) (era 0 → barra colada na borda); sw.js pm-shell-v2. Validação :4534 mobile 375×812: paddingBottom=16px, orientation=landscape-primary, 0 console errors, manifest sem orientation; desktop 1280×720 sem regressão; go test verde. PR #29 aguardando aprovação de merge/restart do :4533.
Run log: 2026-08-09 — verificação foto/capa de categoria: diagnóstico (loja sem capa, artwork exige JWT, cache sujo) + teste e2e de upload na "Cristão" (OK, foto deixada aplicada) + fix backend rota pública + fix loja + 2 testes novos + suíte 57 passed/1 skipped/0 failed em :4534. Branch fix/categoria-foto-loja aguardando aprovação de merge/restart do :4533.
