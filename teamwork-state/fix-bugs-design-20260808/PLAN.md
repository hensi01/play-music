# Teamwork Plan
## Tasks
- [x] [G1] M1 — Baseline: subir o servidor Go (go build -o play-music . && ./play-music em background) escutando na porta 4533; go build ./... e go vet ./... e go test ./... verdes; diagnóstico Playwright em todas as rotas (login cliente (99) 99999-9999, admin via credenciais do .env, home, busca, biblioteca, loja); capturar console errors; escrever inventário de bugs reproduzidos em REPORT.md. — CONCLUÍDA: servidor UP (PID 24836), build/vet/test verdes, diagnóstico completo, inventário B1-B5 + B-NEW-1..4 + desmentidos no REPORT.md (REVIEW PASS, CRITIC BLOCK→correção documental aplicada).
- [x] [G2] M2 — Fixes backend Go: auditoria de status codes (500→4xx), validação de payloads (handleStoreRegister, handleAdminSongs, handleHome/handleSearch, handleUploadSong com limite 15MB, handleStream Range/206, handleArtwork); garantir contratos JSON consumidos por admin.js; adicionar testes Go para bugs corrigidos. — CONCLUÍDA: 5 fixes (B2 RETURNING created_at, B-NEW-1 404 API/assets, B-NEW-3 liked 404, B-NEW-4 cleanup órfão, B-NEW-2 validação categoryIds) + 8 testes novos; go build/vet/test 12/12 PASS (integração Postgres real); evidência HTTP completa. REVIEW PASS.
- [x] [G2] M3 — Fixes frontend JS: try/catch em removePlaylistTrack (app.js:777-780) e toggleLike com sync de player.setLiked (app.js:288-301), catch em toggleLikeCurrent (app.js:1311-1319), guard de teclado excluindo BUTTON/A do target (app.js:1578-1611), race guard no refresh do admin.js (admin.js:24-32), sync de aria-pressed + phone no payload do userForm (admin.js:101-176), disable em save() de categoria (admin.js:410-422), bindLogin pós-render sem listeners duplicados (loja.html:97-103), corrigir do/while de shuffle/repeat com queue.length===1 (player.js:221-224); node --check limpo em app.js/player.js/admin.js/api.js/pwa.js/sw.js; zero console errors. — CONCLUÍDA (retry 1/3): 7 fixes + jwtExpiry com padding base64url (token real validado no browser, 0 erros); suíte m3-validate.js 12/12 PASS; node --check 6/6. RE-REVIEW PASS.
- [x] [G2] M4 — Polimento visual profissional: incrementos em cima dos tokens existentes do style.css (2 camadas, clamp, :focus-visible, transições ≤0.25s) — não reinventar design system; preservar contraste WCAG AA, [data-theme="light"], body.loja, aliases (--bg, --surface, --grid, --faint) e responsivo 375px (scrollWidth <= innerWidth). — CONCLUÍDA: bloco aditivo M4 (326 insertions, 0 deletions) antes de prefers-reduced-motion (última regra); tokens/allowlist 30/30/contraste preservados (fix --accent-2 #15803d 5.02:1 no light); vision 8.5/10; responsivo 375px OK; CSS servido confirmado. REVIEW PASS.
- [x] [G3] M5 — Suíte Playwright completa em e2e/ com playwright.config usando webServer para gerenciar o servidor Go (porta 4533): specs de login (admin+cliente), home/busca/biblioteca, player (play/pause/seek/volume/next/prev), loja (checkout), admin CRUD usuário/categoria, responsivo 375px, PWA/sw; zero console errors; verificação final go test ./... + go vet + console limpo. — CONCLUÍDA: e2e/ com 8 specs + helpers + config (reuseExistingServer); 49 passed / 1 skipped intencional / 0 failed; 2 bugs de produto revelados e corrigidos (a11y aria-valuemax/now no updateBarInPlace; data-icon consistente do vol-icon); verificação final go build/vet/test verdes + node --check 6/6. REVIEW PASS.
